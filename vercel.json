{
  "version": 2,
  "buildCommand": "pnpm --filter @homework-tracker/shared-types build && pnpm --filter @homework-tracker/shared-errors build && pnpm --filter @homework-tracker/shared-auth build && pnpm --filter @homework-tracker/api build && pnpm --filter @homework-tracker/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install",
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
